import React from "react";
import "./loading.styles.scss";

import { IonIcon } from "@ionic/react";
import { logoReact } from "ionicons/icons";
const Loading = () => {
  return <IonIcon icon={logoReact} id="loading" />;
};

export default Loading;
